<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-yellow-50 min-h-screen">
    <nav class="w-full flex flex-wrap items-center justify-between px-4 md:px-8 py-4 bg-white/80 shadow-md  top-0">
        <div class="flex items-center gap-2">
            <img src="/images/1bag.png" alt="BagShop Logo"
                class="h-8 w-8 rounded-full shadow border border-yellow-300" />
            <h3 class="text-xl md:text-2xl font-extrabold text-yellow-700 tracking-wide drop-shadow">Elitebags-Shop</h3>
        </div>
        <button
            class="md:hidden flex items-center px-3 py-2 border rounded text-yellow-700 border-yellow-300 hover:bg-yellow-100 transition"
            onclick="document.getElementById('mobile-menu').classList.toggle('hidden')">
            <i class="ri-menu-line text-2xl"></i>
        </button>
        <% var loggedin =  typeof loggedin !== "undefined" ? loggedin : true %>
        <% if (loggedin) { %>
        <div id="mobile-menu"
            class="w-full md:w-auto flex-col md:flex-row flex gap-4 md:gap-6 items-center md:items-center md:flex hidden md:flex">
            <a class="text-gray-700 hover:text-yellow-700 font-medium transition text-lg" href="/shop">Shop</a>
            <a class="text-gray-700 hover:text-yellow-700 font-medium transition text-lg" href="/cart">Cart</a>
            <a class="text-gray-700 hover:text-yellow-700 font-medium transition text-lg" href="/owners/admin">Add Product</a>
            <a class="text-red-600 hover:text-red-500 font-medium transition text-lg" href="/users/logout">Logout</a>
        </div>
        <% } %>
    </nav>

    <script>
    // Simple mobile menu toggle for Tailwind
    // (If you use Alpine.js or similar, you can replace this with a more robust solution)
    </script>
</body>

</html>